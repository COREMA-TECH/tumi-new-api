import { GraphQLInt } from 'graphql';

const PhaseWorkOrderfields = {
    userId: {
        type: (GraphQLInt)
    },

    phaseworkOrderId: {
        type: (GraphQLInt)
    }
};

export default PhaseWorkOrderfields;
